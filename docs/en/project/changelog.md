# Changelog

This page shows the project's `CHANGELOG.md` exactly as it ships in the
repository. It is included at build time rather than copied, so the
published notes cannot drift from the released ones.

The format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and the project uses [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

---

--8<-- "CHANGELOG.md:10"
